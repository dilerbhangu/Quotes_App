import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF1E1E1E);
  static const Color accent = Color(0xFFE55A54); // A warm accent color like the love logo
  static const Color background = Color(0xFFF0F0F0); // Gray white
  static const Color surface = Colors.green;
  static const Color textPrimary = Color(0xFF111111);
  static const Color textSecondary = Color(0xFF555555);
  
  // Overlay colors
  static const Color overlayDark = Colors.black45;
  static const Color overlayLight = Colors.white24;
}
